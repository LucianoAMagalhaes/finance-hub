# Template of the environment variables this project expects.
# Copy this file to .env and fill in the values:
#   cp .env.example .env

# Database — matches the credentials in docker-compose.yml.
DATABASE_URL="postgresql://finra:finra@localhost:5432/finra"

# Phase 2 — market data
# Nothing to fill in: the "Atualizar cotações" button reads Yahoo Finance, which
# needs no key and no account (ADR-010). If that endpoint ever breaks, the plan B
# recorded in the ADR is brapi.dev, and a BRAPI_TOKEN comes back here.
